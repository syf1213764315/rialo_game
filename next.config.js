/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js lint/build tooling may mutate experimental flags.
  experimental: {},
}

module.exports = nextConfig
